{
  "name": "@devtools/web-tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:test": "playwright test",
    "e2e:test": "start-server and test \"(cd ../../apps/web && npm run start)\" http://localhost:3000 \"playwright test\""
  },
  "dependencies": {
    "@devtools/web": "workspace:*",
    "start-server-and-test": "^1.14.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.12.3"
  }
}
